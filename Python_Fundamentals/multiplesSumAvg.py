for value in range(1, 1001):    #setting a var for the for loop and defining range
    # print "number - ", value    #printing the for loop results

    y = 5   #setting a var so I can extract correct multiples
for numbers in range(5, 1000001): #creating a for loop to cycle through range
    if numbers % 5 == 0:    #fleshing out the multiples of 5 with modulo
        # print numbers   #printing results


        a = [1, 2, 5, 10, 255, 3]   #numbers of var a list
b = sum(a)  #creating a var for the sum
print b #printing sum

print b / len(a) #finding the length of a and making it the divisor of b
